# دعم اللغة العربية وRTL في التطبيق

## 📋 الملخص
تم إضافة دعم كامل للغة العربية في التطبيق مع دعم التخطيط من اليمين إلى اليسار (RTL) لتحسين تجربة المستخدمين الناطقين بالعربية.

<img width="796" height="935" alt="image" src="https://github.com/user-attachments/assets/fc32a055-3a58-429a-8946-caad559363d1" />


## ✨ الميزات المضافة

### 1. دعم RTL (Right-to-Left)
- إضافة `dir="rtl"` تلقائياً لجميع صفحات HTML عند استخدام اللغة العربية
- تحديث فئات Tailwind CSS للحدود والهوامش لدعم RTL
- تعديل موضع Sidebar والتنقل ليتوافق مع التخطيط العربي
- دعم اللغات RTL الأخرى: العبرية، الفارسية، الأردية

### 2. ملفات الترجمة العربية
- إنشاء ملف `lang/ar.json` يحتوي على ترجمات عربية شاملة
- ترجمة جميع النصوص في التطبيق:
  - صفحات المصادقة (تسجيل الدخول، التسجيل، إعادة تعيين كلمة المرور)
  - لوحة التحكم والقوائم
  - صفحات الإعدادات (الملف الشخصي، كلمة المرور، المصادقة الثنائية)
  - الرسائل والتأكيدات

### 3. دعم الخطوط العربية
- إضافة خط Cairo من Google Fonts للعربية
- تطبيق الخط تلقائياً على صفحات RTL
- تحسين عرض النصوص العربية

### 4. تحديث الإعدادات
- تعيين اللغة الافتراضية إلى العربية في `config/app.php`
- تحديث `fallback_locale` إلى العربية

## 🔧 الملفات المعدلة

### ملفات التخطيط (Layouts)
- `resources/views/components/layouts/app/sidebar.blade.php`
- `resources/views/components/layouts/app/header.blade.php`
- `resources/views/components/layouts/auth/simple.blade.php`
- `resources/views/components/layouts/auth/split.blade.php`
- `resources/views/components/layouts/auth/card.blade.php`

### ملفات CSS والخطوط
- `resources/css/app.css` - إضافة دعم الخطوط العربية
- `resources/views/partials/head.blade.php` - إضافة خط Cairo

### ملفات الإعدادات والترجمة
- `config/app.php` - تحديث اللغة الافتراضية
- `lang/ar.json` - ملف الترجمة العربية الجديد

## 🎨 التحسينات البصرية

### التخطيط RTL
- تبديل الحدود من `border-e` إلى `border-s` في وضع RTL
- تعديل الهوامش (`ms` ↔ `me`) حسب الاتجاه
- تحديث موضع Sidebar Toggle (`inset="left"` → `inset="right"`)
- تحسين المسافات والتباعد للعناصر

### الخطوط
- استخدام خط Cairo للعربية
- تحسين قابلية القراءة للنصوص العربية

## 📝 التغييرات التقنية

### فئات Tailwind المحدثةe
<!-- قبل -->
class="border-e me-5 ms-2"

<!-- بعد -->
class="{{ in_array(app()->getLocale(), ['ar', 'he', 'fa', 'ur']) ? 'border-s' : 'border-e' }} {{ in_array(app()->getLocale(), ['ar', 'he', 'fa', 'ur']) ? 'ms-5' : 'me-5' }}"### إضافة dir attribute
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}" dir="{{ in_array(app()->getLocale(), ['ar', 'he', 'fa', 'ur']) ? 'rtl' : 'ltr' }}" class="dark">## 🧪 الاختبار

### ما تم اختباره
- ✅ عرض الواجهة بشكل صحيح في وضع RTL
- ✅ ترجمة جميع النصوص إلى العربية
- ✅ عمل التنقل والقوائم بشكل صحيح
- ✅ عرض الخطوط العربية بشكل صحيح
- ✅ تنسيق الكود باستخدام Laravel Pint

## 📦 المتطلبات

لا توجد متطلبات إضافية. جميع التغييرات متوافقة مع:
- Laravel 12
- Tailwind CSS v4
- Livewire 3
- Flux UI

## 🚀 كيفية الاستخدام

1. تأكد من أن `APP_LOCALE=ar` في ملف `.env`
2. مسح الكاش إذا لزم الأمر:
   php artisan config:clear
   php artisan cache:clear
   3. إعادة بناء الأصول الأمامية:
   npm run build
   # أو
   npm run dev
   ## 📸 لقطات الشاشة

- [ ] إضافة لقطات شاشة للواجهة العربية

## 🔄 التغييرات المستقبلية

- [ ] إضافة دعم لغات RTL إضافية إذا لزم الأمر
- [ ] تحسين ترجمات معينة بناءً على ملاحظات المستخدمين
- [ ] إضافة خيار تبديل اللغة في الواجهة

## 📚 المراجع

- [Laravel Localization Documentation](https://laravel.com/docs/localization)
- [Tailwind CSS RTL Support](https://tailwindcss.com/docs/hover-focus-and-other-states#rtl-support)
- [Arabic Typography Best Practices](https://www.w3.org/International/articles/typography/arabic)

---]
